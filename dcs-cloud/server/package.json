{
  "name": "dcs-cloud-mcp-server",
  "version": "2.0.2",
  "description": "Bootstrap-only MCP Server for the DCS Cloud CLI (dcs): download + auto-update + login; the host AI runs the CLI directly",
  "type": "module",
  "main": "index.js",
  "bin": {
    "dcs-cloud-mcp-server": "index.js"
  },
  "files": [
    "index.js"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
